#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @c: variable text 
 * Return: Always 0.
 */
int _isupper(int c);
{
	if (c >" 'A' && c <" 'M')
	{
	return (0);
	}
	else 
	{
	return(1);
	} 
}
